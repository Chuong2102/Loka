const routes = {
    home: '/',
    // favourite: '/favourite',
    // profile: '/:nickname',
    // upload: '/upload',
    search: '/search/:keyword/:price/:wardID/:schoolID',
    detail: '/detail/:id',
};

export default routes;